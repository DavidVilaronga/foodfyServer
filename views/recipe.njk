{%extends "layout.njk"%}

{%block header%}

        <header>
            <div class="header">
                <a href="/" class="logo"><img src="./assets/logo.png"  alt="logo"></a>
                <a href="about" class="sobre">Sobre</a>
                <a href="recipe" class="sobre_bold">Receitas</a>
            </div>
            
        </header>

        {%endblock%}

        {%block content%}


        
        <div class="content">

        {% for item in items %}

            <div class="receita" id="{{item.id}}">
                <img src="./assets/{{item.id}}.png" alt="{{item.recipe}}">
                <h1>{{item.recipe}}</h1>
                <h2>{{item.by}}</h2>
            </div>
        {%endfor%}
        
        </div>

        <div class="content">

        {% for item in items %}

            <div class="receita" id="{{item.id}}">
                <img src="./assets/{{item.id}}.png" alt="{{item.recipe}}">
                <h1>{{item.recipe}}</h1>
                <h2>{{item.by}}</h2>
            </div>
        {%endfor%}
        
        </div>
            
        
        {%endblock%}
   